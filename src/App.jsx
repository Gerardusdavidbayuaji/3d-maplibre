const TOKEN = process.env.ACCESS_TOKEN;
import { Tile3DLayer } from "@deck.gl/geo-layers";

const App = () => {
  return (
    <div className="flex justify-center items-center text-center w-full h-screen bg-gray-400">
      Kolom Map
    </div>
  );
};

export default App;
